[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "intrusion-detec-sys"
version = "1.0.0"
description = "Defensive network and host intrusion detection system with a live dashboard."
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "Igor Kuchenbecker" }]
keywords = ["ids", "security", "detection-engineering", "soc", "network-security"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: System :: Networking :: Monitoring",
]
dependencies = [
    "scapy>=2.5,<3",
    "flask>=3.0,<4",
    "rich>=13,<16",
]

[project.optional-dependencies]
tui = ["textual>=1.0,<9"]
dev = ["pytest>=8,<10", "pytest-cov>=5", "ruff>=0.6,<0.7", "textual>=1.0,<9"]

[project.scripts]
ids = "ids.cli.commands:main"
ids-console = "ids.tui:main"

[project.urls]
Repository = "https://github.com/igorkuchenbecker/intrusion-detec-sys"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# Every non-Python file under dashboard/, listed by directory rather than by
# extension. The previous globs named css and js and silently omitted
# static/favicon.svg, so an installed wheel served the dashboard with a 404 for
# its own icon -- invisible to every test, because they all ran against the
# working tree where the file is on disk. The wheel-install CI job now covers
# this; the globs cover the whole directory so the next asset added does not
# need anyone to remember.
"ids.dashboard" = ["templates/*", "static/*", "static/css/*", "static/js/*"]
"ids.tui" = ["*.tcss"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "D"]
ignore = [
    "D203",  # conflicts with D211 (no blank line before class docstring)
    "D213",  # conflicts with D212 (multi-line summary on first line)
    "D105",  # magic methods are documented by their standard behaviour
    "D107",  # __init__ is covered by the class docstring
    "N818",  # exception names are clear and rooted at a single IDSError base
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]
